## **I. Основы Node.js**

### 1. **Введение и архитектура**

1. Что такое Node.js, и почему эта платформа стала популярной?
2. На каком движке работает Node.js? Какие преимущества он даёт?
3. В каких сферах может применяться Node.js, кроме веб-серверов?
4. В чем преимущество использования JavaScript на сервере?
5. Почему Node.js считается кроссплатформенной средой?
6. Какие ключевые особенности сделали Node.js популярным?
7. Почему код в Node.js работает быстрее, чем в Python или Ruby?
8. Как неблокирующая архитектура влияет на производительность Node.js?
9. Что такое асинхронность в Node.js? Почему она важна?
10. Как Node.js обрабатывает большое количество одновременных соединений?
11. Почему Node.js использует модель с единственным потоком?
12. Как установить Node.js на разные операционные системы?
13. В каком году появился Node.js, и кто его создал?
14. Что такое IO.js, и почему он был создан?
15. Как появление V8 повлияло на развитие Node.js?
16. Какие ключевые события произошли в экосистеме Node.js с 2009 по 2018 годы?
17. Что произошло с пакетом `left-pad`, и какие выводы были сделаны из этого инцидента?
18. В чем основные различия между браузерным JavaScript и Node.js?
19. Почему в Node.js нет объектов `window` и `document`?
20. Какие API предоставляет браузер, но отсутствуют в Node.js?
21. Какие модули предоставляет Node.js для работы с файловой системой?

### 2. **Установка и настройка**

1. Как установить Node.js на разные операционные системы?
2. Что такое nvm, и в чем его преимущества?
3. Как запустить простое приложение в Node.js?
4. Какие встроенные модули есть в Node.js? Какие из них самые часто используемые?
5. Как можно получить доступ к переменным окружения в Node.js?
6. Что делает переменная `NODE_ENV`, и зачем она нужна?
7. Как установить переменную окружения перед запуском скрипта?

## **II. JavaScript для Node.js**

### 1. **Основы языка**
    
1. Какие базовые концепции JavaScript необходимо знать перед изучением Node.js?
2. В чем разница между стрелочными функциями и обычными функциями в JavaScript?
3. Как работает ключевое слово `this` в JavaScript? Чем оно отличается в разных контекстах?
4. Что такое замыкания в JavaScript и как они работают?
5. Какие методы асинхронного программирования существуют в JavaScript?
6. Что такое цикл событий (Event Loop) и как он работает?
7. Что такое цикл событий (Event Loop) в JavaScript, и зачем он нужен?
8. Почему JavaScript является однопоточным, и какие преимущества это даёт?
9. Какие проблемы могут возникнуть при блокировке цикла событий?
10. Как работает стек вызовов (Call Stack) в JavaScript?
11. Что произойдёт, если в коде есть бесконечный цикл?


### 2. **Асинхронное программирование**


1. Как неблокирующая архитектура влияет на производительность Node.js?
2. Что такое асинхронность в Node.js? Почему она важна?
3. Как Node.js обрабатывает большое количество одновременных соединений?
4. Почему Node.js использует модель с единственным потоком?
5. Что такое асинхронность в JavaScript, и почему она важна?
6. Какие есть способы работы с асинхронным кодом в JavaScript?
7. В чем разница между синхронным и асинхронным кодом?
8. Что такое callback-функции, и как они работают?
9. Какие проблемы связаны с использованием callback-функций?
10. Как можно избежать callback hell?
11. Что такое промисы в JavaScript?
12. Какие состояния бывают у промиса?
13. В чем разница между `resolve` и `reject`?
14. Что делает метод `.then()`, `.catch()` и `.finally()`?
15. Как можно цепочкой обработать несколько асинхронных операций
16. Как работает `async/await`?
17. В чем преимущества `async/await` перед промисами?
18. Как правильно обрабатывать ошибки при использовании `async/await`?
19. Как работает Event Loop в JavaScript?
20. В чем разница между microtasks и macrotasks?
21. Как `setTimeout` и `setImmediate` взаимодействуют с Event Loop?
22. Как Node.js обрабатывает асинхронные операции?
23. В чем разница между асинхронным и многопоточным выполнением?
24. Как `fs.promises` отличается от `fs` с колбэками?
25. Почему важно обрабатывать ошибки в колбэках потоков?
26. Как избежать утечек памяти при работе с `EventEmitter`?**
27. Как потоки помогают снизить нагрузку на память?
28. Какие методы модуля `os` могут помочь в мониторинге производительности?

## **III. Модули и зависимости**

### 1. **CommonJS/ES Modules**


1. В чем разница между CommonJS и ES Modules?
2. Как подключить модуль в Node.js? В чем разница между `require()` и `import`?
3. Какие движки JavaScript существуют, помимо V8?
4. Чем отличается `exports` от `module.exports`?
5. Как импортировать модуль из соседнего файла?
6. Почему нельзя перезаписать `exports` напрямую (например, `exports = {...}`)?
7. Как избежать циклических зависимостей между модулями?
8. Какие механизмы кеширования модулей есть в Node.js?
9. Как загрузить модуль из строки (динамически) без файла?
10. Создайте модуль `math.js`, экспортирующий функции `add` и `multiply`, и используйте его в `app.js`.
11. Какие инструменты хостинга вы бы выбрали для MVP и для высоконагруженного SaaS? Почему?
12. Опишите жизненный цикл Node.js-приложения от разработки до продакшена с использованием PAAS.
13. Что такое npm, и какие основные задачи он решает?
14. В чем разница между локальной и глобальной установкой npm-пакетов?
15. Как узнать, какие пакеты установлены в проекте?
16. Чем отличается `npm list` от `npm list --depth=0`?
17. Как просмотреть список глобально установленных пакетов?
18. Что такое package.json и package-lock.json?
19. Как узнать версию конкретного установленного пакета?
20. Как узнать самую последнюю версию пакета, доступную в npm-репозитории?
21. Как можно установить конкретную версию пакета?
22. Как посмотреть все доступные версии npm-пакета?
23. Как обновить все зависимости в проекте до их последних версий?
24. В чем разница между `npm update` и `npx npm-check-updates -u`?
25. Как удалить установленный npm-пакет?
26. В чем разница между `npm uninstall <package>` и `npm uninstall -g <package>`?
27. Как удалить глобально установленный npm-пакет?
28. Как проверить, какие пакеты в проекте устарели?
29. Почему npm не обновляет зависимости до мажорных версий при запуске `npm update`?
30. Чем отличаются `dependencies` и `devDependencies`?
31. Как установить пакет как зависимость разработки?
32. Как установить зависимости без зависимостей для разработки?
33. Почему для продакшн-среды стоит использовать `npm install --production`?
34. Что такое npx, и чем он полезен?
35. Как с помощью npx выполнить утилиту без её установки?
36. Как запустить скрипт с GitHub с помощью npx?
37. В чем разница между `npx create-react-app` и глобальной установкой `create-react-app`?
38. Как с помощью npx запустить код с разными версиями Node.js?

### 2. **NPM и NPX**

1. **Что такое npm и для чего он используется?**
2. **Как установить все зависимости проекта из `package.json`?**
3. **Чем отличается `npm install lodash` от `npm install -g lodash`?**
4. **Что делает флаг `--save-dev` при установке пакета?**
5. **Как запустить скрипт `start` из `package.json`?**
6. **Что такое `node_modules` и зачем она нужна?**
7. **Как добавить пакет в `dependencies` без ручного редактирования `package.json`?**
8. **Объясните разницу между `dependencies` и `devDependencies`.**  
9. **Что такое семантическое версионирование (semver)?**  
10. **Зачем нужен `package-lock.json`? Почему его нельзя игнорировать в Git?**
11. **Как установить конкретную версию пакета (например, React 16.8.1)?**
12. **Что делает команда `npm update`? Как обновить только один пакет?**
13. **Как указать, что проект требует Node.js версии 14 и выше?**
14. **Как запустить бинарный файл пакета (например, `webpack`), установленного локально?**  
15. **Какие проблемы решает `package-lock.json` и как он взаимодействует с `package.json`?**  _
16. **Как работает алгоритм разрешения зависимостей npm?**  
17. **Как оптимизировать размер `node_modules` в production?**  
18. **Что такое `peerDependencies` и в каких сценариях они используются?**  
    _Пример: Зачем библиотеке React указывать React как peer dependency?_
19. **Как настроить `npm scripts` для многоэтапной сборки (dev, build, test)?**  
20. **Как избежать проблем с глобальными пакетами при работе в команде?**  
21. **Как работает `engines` в `package.json`? Что произойдет, если версия Node.js не соответствует указанной?**
22. **Ситуация**: После `npm install` в проекте появились ошибки. Как быстро откатить зависимости к версиям из `package-lock.json`?  
23. **Объясните**: Почему в `package-lock.json` могут быть несколько версий одного пакета?  
24. Что такое npm? Какие у него основные функции?
25. Какие популярные фреймворки используются с Node.js?
26. В чем отличие Express от Koa?
27. **Как `npx` избегает глобальной установки пакетов?**
28. **Что делает `browserslist` в `package.json` и какие инструменты его используют?**
29. **Как `npm` обрабатывает конфликты зависимостей при использовании `npm link`?**

## **IV. Работа с CLI**

### 1. **Аргументы и ввод**
    
1. Как получить аргументы командной строки в Node.js-скрипте?
2. Что хранится в `process.argv[0]` и `process.argv[1]`?
3. Зачем использовать библиотеку `minimist` для обработки аргументов?
4. Как обработать аргумент вида `--port=3000` без сторонних библиотек?
5. Какие проблемы могут возникнуть при передаче аргументов в формате ключ-значение без валидации?
6. Как организовать обязательную проверку наличия определенного аргумента (например, `--config`)?
7. Напишите скрипт, который принимает аргумент `--env=production` и выводит соответствующее сообщение.
8. Чем отличается `console.error` от `console.log`?
9. Как вывести объект в консоль в формате JSON с помощью `console.log`?
10. Как измерить время выполнения функции с помощью `console.time`?
11. Зачем использовать библиотеку `chalk` вместо escape-последовательностей для цветов?
12. Почему `console.trace()` полезен для отладки асинхронного кода?
13. Как создать кастомный логгер с разделением выводов в `stdout` и `stderr`?
14. Реализуйте прогресс-бар для 10 итераций с задержкой 200 мс, используя библиотеку `progress`.
15. Какой модуль Node.js используется для чтения ввода из командной строки?
16. Чем отличается `readline-sync` от стандартного `readline`?
17. Зачем использовать библиотеку `inquirer` вместо `readline`?
18. Как безопасно запросить пароль без отображения вводимых символов?
19. Какие проблемы могут возникнуть при использовании `process.stdin` без обработки потоков?
20. Как реализовать меню с выбором вариантов (radio buttons) через `inquirer`?
21. Какие способы завершения работы Node.js-приложения существуют?
22. Какой метод позволяет программно завершить выполнение Node.js-приложения? Какие у него есть недостатки?
23. Что такое сигналы POSIX, и какие из них можно использовать в Node.js?
24. В чем разница между `SIGTERM` и `SIGKILL`?
25. Как безопасно завершить работу HTTP-сервера в Node.js?

### 2. **REPL**

1. Как запустить Node.js в режиме REPL?
2. Для чего используется переменная `_` в REPL?
3. Какая команда в REPL позволяет сохранить сессию в файл?
4. Как исследовать методы глобального объекта `Buffer` через REPL?
5. Чем отличается выполнение кода через `.editor` от ввода многострочного выражения без него?
6. Как прервать выполнение многострочного выражения в REPL?
7. Какие ограничения есть у REPL для отладки сложных приложений?
8. Как загрузить внешний модуль (например, `lodash`) в текущую сессию REPL?
9. Напишите в REPL цикл, который выводит числа от 1 до 5, используя `_` для доступа к предыдущему результату.

## **V. Сетевые взаимодействия**

### 1. **HTTP/WebSocket**

1. Как работает модуль `http` в Node.js?
2. Что происходит, когда пользователь вводит URL в адресную строку браузера и нажимает Enter?
3. Что такое DNS и какую роль он играет в процессе выполнения HTTP-запроса?
4. Как браузер определяет IP-адрес домена? Как работает кэширование DNS?
5. В чем разница между протоколами TCP и UDP? Почему DNS-запросы выполняются через UDP?
6. Как работает установление TCP-соединения (трехфазное рукопожатие)?
7. Какие основные части содержит HTTP-запрос?
8. Какие бывают методы HTTP-запросов и чем они отличаются?
9. Какие коды состояния HTTP вы знаете? Приведите примеры и объясните их назначение.
10. Чем отличаются HTTP/1.1, HTTP/2 и HTTP/3?
11. Что такое заголовки HTTP-запроса? Какие заголовки чаще всего используются?
12. Какие модули в Node.js используются для выполнения HTTP-запросов?
13. Как выполнить HTTP GET-запрос в Node.js с использованием встроенного модуля `https`?
14. Как выполнить POST-запрос в Node.js с использованием модуля `https`?
15. Как можно обработать ошибки при выполнении HTTP-запросов в Node.js?
16. Чем удобен `axios` по сравнению с встроенным модулем `https`?
17. Как выполнить GET-запрос с параметрами, используя `axios`?
18. Как прервать HTTP-запрос в `axios`?
19. Какие преимущества у `axios` перед `fetch`?
20. Какой заголовок HTTP-запроса отвечает за передачу токена авторизации?
21. Как реализовать обработку ответа сервера в `axios`?
22. Чем WebSocket отличается от HTTP?
23. Какие преимущества дает WebSocket по сравнению с REST API?
24. Как создать WebSocket-соединение в Node.js?
25. Какие события существуют у WebSocket-соединения и для чего они используются?
26. Чем отличается `ws://` от `wss://`?
27. Как сервер может отправлять сообщения клиенту в WebSocket?
28. Какие библиотеки используются для работы с WebSocket в Node.js?
29. Как обработать ошибки при работе с WebSocket?
30. Какие задачи хорошо решаются с использованием WebSocket?
31. Как организовать авторизацию пользователя при работе с WebSocket?
32. Как создать HTTP-сервер в Node.js? Приведите пример кода.
33. Чем отличаются методы `http.get()` и `http.request()`?
34. Какие HTTP-статусы соответствуют кодам 404, 500, 200?
35. Что такое `http.Agent` и как он управляет соединениями?
36. Как получить HTTP-метод (GET, POST) из входящего запроса?
37. Какие данные доступны в объекте `http.IncomingMessage`?
38. Напишите сервер, который возвращает текущее время при GET-запросе на `/time`.
39. Как обработать POST-запрос и прочитать тело запроса?
40. Какие типы потоков существуют в Node.js? Чем они отличаются?

### 2. **Сетевые протоколы**
    
1. Что происходит, когда пользователь вводит URL в адресную строку браузера и нажимает Enter?
2. Что такое DNS и какую роль он играет в процессе выполнения HTTP-запроса?
3. Как браузер определяет IP-адрес домена? Как работает кэширование DNS?
4. В чем разница между протоколами TCP и UDP? Почему DNS-запросы выполняются через UDP?
5. Как работает установление TCP-соединения (трехфазное рукопожатие)?
6. Какие основные части содержит HTTP-запрос?
7. Какие бывают методы HTTP-запросов и чем они отличаются?
8. Какие коды состояния HTTP вы знаете? Приведите примеры и объясните их назначение.
9. Чем отличаются HTTP/1.1, HTTP/2 и HTTP/3?
10. Что такое заголовки HTTP-запроса? Какие заголовки чаще всего используются?
11. Какие модули в Node.js используются для выполнения HTTP-запросов?
12. Как выполнить HTTP GET-запрос в Node.js с использованием встроенного модуля `https`?
13. Как выполнить POST-запрос в Node.js с использованием модуля `https`?
14. Как можно обработать ошибки при выполнении HTTP-запросов в Node.js?
15. Чем удобен `axios` по сравнению с встроенным модулем `https`?
16. Как выполнить GET-запрос с параметрами, используя `axios`?
17. Как прервать HTTP-запрос в `axios`?
18. Какие преимущества у `axios` перед `fetch`?
19. Какой заголовок HTTP-запроса отвечает за передачу токена авторизации?
20. Как реализовать обработку ответа сервера в `axios`?
21. Чем WebSocket отличается от HTTP?
22. Какие преимущества дает WebSocket по сравнению с REST API?
23. Как создать WebSocket-соединение в Node.js?
24. Какие события существуют у WebSocket-соединения и для чего они используются?
25. Чем отличается `ws://` от `wss://`?
26. Как сервер может отправлять сообщения клиенту в WebSocket?
27. Какие библиотеки используются для работы с WebSocket в Node.js?
28. Как обработать ошибки при работе с WebSocket?
29. Какие задачи хорошо решаются с использованием WebSocket?
30. Как организовать авторизацию пользователя при работе с WebSocket?

## **VI. Работа с файлами**

### 1. **Файловая система**
    
1. Что такое файловый дескриптор в Node.js, и зачем он нужен?
2. Как открыть файл в Node.js с помощью `fs.open()`? Какие параметры принимает этот метод?
3. В чем разница между `fs.open()` и `fs.openSync()`?
4. Какие флаги можно использовать при открытии файла и что они означают (`r`, `r+`, `w+`, `a`, `a+` и т. д.)?
5. Какие ошибки могут возникнуть при открытии файла и как их обрабатывать?
6. Как получить информацию о файле в Node.js? Какие методы для этого существуют?
7. Какие свойства можно получить из объекта `stats`?
8. Как определить, является ли путь файлом, директорией или символической ссылкой?
9. В чем разница между `fs.stat()`, `fs.lstat()` и `fs.fstat()`?
10. Как работает модуль `path` в Node.js? Почему он полезен?
11. Как получить директорию, имя файла и расширение из пути (`path.dirname()`, `path.basename()`, `path.extname()`)?
12. В чем разница между `path.join()` и `path.resolve()`?
13. Что делает `path.normalize()`?
14. Как проверить, является ли путь абсолютным (`path.isAbsolute()`) и зачем это нужно?
15. Какие методы в `fs` используются для чтения файла?
16. В чем разница между `fs.readFile()` и `fs.readFileSync()`?
17. Как можно задать кодировку при чтении файла?
18. Чем потоковое чтение файлов лучше метода `fs.readFile()` при работе с большими файлами?
19. Как записать данные в файл? Какие флаги можно использовать для управления режимами записи (`fs.writeFile()`)?
20. Как дописать данные в файл (`fs.appendFile()`, `fs.appendFileSync()`)?
21. Как проверить, существует ли директория (`fs.access()`, `fs.existsSync()`) и какие проблемы могут возникнуть?
22. Как создать директорию? В чем разница между `fs.mkdir()` и `fs.mkdirSync()`?
23. Как прочитать содержимое директории (`fs.readdir()`, `fs.readdirSync()`)?
24. Как удалить директорию? Почему `fs.rmdir()` не удаляет непустые папки, и как это можно обойти?
25. Как рекурсивно удалить директорию в Node.js? Какие альтернативные пакеты можно использовать?
26. Чем `fs.promises` отличается от стандартных методов `fs`?
27. В чем разница между `fs.watchFile()` и `fs.watch()`?
28. Как работает `fs.createReadStream()` и `fs.createWriteStream()`, и зачем их использовать?
29. Какие методы модуля `fs` блокируют главный поток? Почему стоит избегать их в асинхронных приложениях?
30. Как можно безопасно работать с файлами в многопоточной среде (например, при одновременной записи в файл несколькими процессами)?
31. Напишите код, который читает файл и отправляет его содержимое в ответ HTTP-сервера через поток.
32. Создайте дуплексный поток, который логирует все полученные данные.
33. Как установить соединение с MySQL в Node.js? Какие параметры обязательны?
34. Как выполнить запрос `SELECT * FROM users WHERE id = ?` с параметром?
35. Как безопасно закрыть соединение с базой данных?
36. Как обработать SQL-инъекции при использовании пакета `mysqljs/mysql`?
37. Что возвращает метод `connection.query()` при вставке данных (INSERT)?
38. Как настроить пул соединений с MySQL?
39. Напишите код, который добавляет новую запись в таблицу `todos` и возвращает её ID.
40. Реализуйте обработку ошибки при подключении к несуществующей базе данных.

### 2. **Потоки (Streams)**

1. Какие типы потоков существуют в Node.js? Чем они отличаются?
2. Что делает метод `pipe()`? Приведите пример использования.
3. Какие преимущества дают потоки при работе с большими файлами?
4. Как создать кастомный `Transform`-поток, который преобразует данные в верхний регистр
5. Что произойдет, если в `Writable`-потоке не вызвать `next()` в методе `_write()`?
6. Как обработать ошибку в потоке?
7. Как потоки помогают снизить нагрузку на память?

## **VII. Системные утилиты**

### 1. **Модуль `os`**
    
1. Как получить информацию о процессорах системы в Node.js?
2. Что возвращает метод `os.endianness()` и зачем это нужно?
3. Как определить домашнюю директорию текущего пользователя с помощью Node.js?**
4. Чем отличаются значения `os.EOL` в Windows и POSIX-системах? Приведите примеры использования.
5. Как получить список сигналов процесса, доступных в ОС, через Node.js? Для чего это может пригодиться?
6. Какие данные содержит объект, возвращаемый `os.networkInterfaces()`? Приведите пример структуры.
7. Напишите код, который выводит общий объем памяти и свободную память в гигабайтах.
8. Как определить, работает ли приложение на Windows, используя модуль `os`?
9. Какие методы модуля `os` могут помочь в мониторинге производительности?


### 2. **Процессы и сигналы**

1. Какие способы завершения работы Node.js-приложения существуют?
2. Какой метод позволяет программно завершить выполнение Node.js-приложения? Какие у него есть недостатки?
3. Что такое сигналы POSIX, и какие из них можно использовать в Node.js?
4. В чем разница между `SIGTERM` и `SIGKILL`?
5. Как безопасно завершить работу HTTP-сервера в Node.js?
6. Как получить список сигналов процесса, доступных в ОС, через Node.js? Для чего это может пригодиться?

## **VIII. Профилирование и деплой**

### 1. **Хостинг и продакшн**
    
1. Какие существуют варианты хостинга для быстрого тестирования Node.js-приложения?
2. В чем преимущество использования локального туннеля (например, ngrok) для демонстрации приложения?
3. Назовите две бессерверные (FAAS) платформы для развертывания Node.js-функций.
4. Чем PAAS-решения (например, Heroku) отличаются от VPS-хостинга?
5. Какие ограничения есть у бесплатной версии ngrok и как их можно обойти?
6. Почему бессерверные платформы (FAAS) могут быть неудобны для долгосрочных проектов?
7. Как выбрать между VPS и PAAS для высоконагруженного приложения? Какие критерии важны?
8. Какие преимущества дает использование AWS Elastic Beanstalk по сравнению с обычным VPS?
9. Как обеспечить безопасность при использовании локального туннеля (ngrok) в продакшене?
10. Опишите шаги для развертывания приложения на Heroku через CLI.
11. Как настроить HTTPS для пользовательского домена на платформе вроде Glitch?
12. Как установить переменную окружения `NODE_ENV` в production?
13. Какие оптимизации обычно применяются в продакшн-среде для Express?
14. Зачем нужен `express.errorHandler` и как он настраивается для разных сред?
15. Как поведение кэширования шаблонов в Express зависит от `NODE_ENV`?
16. Почему в продакшне рекомендуется минимизировать логирование?
17. Как использовать разные конфигурации для staging и production?

### 2. **Безопасность**

1. Как обеспечить безопасность при использовании локального туннеля (ngrok) в продакшене?
2. Как обработать SQL-инъекции при использовании пакета `mysqljs/mysql`?

## **IX. Расширенные концепции**

### 1. **EventEmitter**

1. Что такое `EventEmitter`? Как создать его экземпляр?
2. Чем отличаются методы `emitter.on()` и `emitter.once()`?
3. Как удалить все обработчики для события `open` из `EventEmitter`
4. Что такое события `newListener` и `removeListener`? Приведите пример их использования.
5. Как ограничить максимальное количество обработчиков для события в `EventEmitter`?
6. Зачем нужны методы `prependListener()` и `prependOnceListener()`? В чем их отличие от `on()`?
7. Создайте `EventEmitter`, который генерирует событие `data` каждую секунду, и обработайте его.
8. Напишите код, который подсчитывает количество обработчиков для события `error`.

### 2. **Ошибки и оптимизация**

1. Зачем нужен `express.errorHandler` и как он настраивается для разных сред?
2. Как поведение кэширования шаблонов в Express зависит от `NODE_ENV`?
3. Почему в продакшне рекомендуется минимизировать логирование?
4. Как использовать разные конфигурации для staging и production?

## **X. Тестирование и отладка**

### Инструменты**

1. Какие проблемы могут возникнуть при передаче аргументов в формате ключ-значение без валидации?
2. Как организовать обязательную проверку наличия определенного аргумента (например, `--config`)?
3. Напишите скрипт, который принимает аргумент `--env=production` и выводит соответствующее сообщение.
4. Чем отличается `console.error` от `console.log`?
5. Как вывести объект в консоль в формате JSON с помощью `console.log`?
6. Как измерить время выполнения функции с помощью `console.time`?
7. Зачем использовать библиотеку `chalk` вместо escape-последовательностей для цветов?
8. Почему `console.trace()` полезен для отладки асинхронного кода?
9. Как создать кастомный логгер с разделением выводов в `stdout` и `stderr`?
10. Реализуйте прогресс-бар для 10 итераций с задержкой 200 мс, используя библиотеку `progress`.
11. Какой модуль Node.js используется для чтения ввода из командной строки?
12. Какие ограничения есть у REPL для отладки сложных приложений?
13. Почему `console.trace()` полезен для отладки асинхронного кода?




### Others



1. Что такое Node.js, и почему эта платформа стала популярной?
2. На каком движке работает Node.js? Какие преимущества он даёт?
3. В каких сферах может применяться Node.js, кроме веб-серверов?
4. В чем преимущество использования JavaScript на сервере?
5. Почему Node.js считается кроссплатформенной средой?
6. Какие ключевые особенности сделали Node.js популярным?
7. Почему код в Node.js работает быстрее, чем в Python или Ruby?
8. Как неблокирующая архитектура влияет на производительность Node.js?
9. Что такое асинхронность в Node.js? Почему она важна?
10. Как Node.js обрабатывает большое количество одновременных соединений?
11. Почему Node.js использует модель с единственным потоком?
12. Как установить Node.js на разные операционные системы?
13. Что такое nvm, и в чем его преимущества?
14. Как запустить простое приложение в Node.js?
15. Какие встроенные модули есть в Node.js? Какие из них самые часто используемые?
16. Как работает модуль `http` в Node.js?
17. Что такое npm? Какие у него основные функции?
18. Какие популярные фреймворки используются с Node.js?
19. В чем отличие Express от Koa?
20. Что такое Next.js, и зачем он нужен?
21. Для чего используется библиотека Socket.io?
22. Почему Meteor считается фулстек-фреймворком?
23. В каком году появился Node.js, и кто его создал?
24. Что такое IO.js, и почему он был создан?
25. Как появление V8 повлияло на развитие Node.js?
26. Какие ключевые события произошли в экосистеме Node.js с 2009 по 2018 годы?
27. Что произошло с пакетом `left-pad`, и какие выводы были сделаны из этого инцидента?
28. Какие базовые концепции JavaScript необходимо знать перед изучением Node.js?
29. В чем разница между стрелочными функциями и обычными функциями в JavaScript?
30. Как работает ключевое слово `this` в JavaScript? Чем оно отличается в разных контекстах?
31. Что такое замыкания в JavaScript и как они работают?
32. Какие методы асинхронного программирования существуют в JavaScript?
33. Что такое цикл событий (Event Loop) и как он работает?
34. В чем основные различия между браузерным JavaScript и Node.js?
35. Почему в Node.js нет объектов `window` и `document`?
36. Какие API предоставляет браузер, но отсутствуют в Node.js?
37. Какие модули предоставляет Node.js для работы с файловой системой?
38. В чем разница между CommonJS и ES Modules?
39. Как подключить модуль в Node.js? В чем разница между `require()` и `import`?
40. Какие движки JavaScript существуют, помимо V8?
41. Какую роль играет движок V8 в Node.js?
42. Как JavaScript-код выполняется в движке V8? В чем разница между интерпретацией и компиляцией?
43. Какие способы завершения работы Node.js-приложения существуют?
44. Какой метод позволяет программно завершить выполнение Node.js-приложения? Какие у него есть недостатки?
45. Что такое сигналы POSIX, и какие из них можно использовать в Node.js?
46. В чем разница между `SIGTERM` и `SIGKILL`?
47. Как безопасно завершить работу HTTP-сервера в Node.js?
48. Как можно получить доступ к переменным окружения в Node.js?
49. Что делает переменная `NODE_ENV`, и зачем она нужна?
50. Как установить переменную окружения перед запуском скрипта?
51. Какие существуют варианты хостинга для быстрого тестирования Node.js-приложения?
52. В чем преимущество использования локального туннеля (например, ngrok) для демонстрации приложения?
53. Назовите две бессерверные (FAAS) платформы для развертывания Node.js-функций.
54. Чем PAAS-решения (например, Heroku) отличаются от VPS-хостинга?
55. Какие ограничения есть у бесплатной версии ngrok и как их можно обойти?
56. Почему бессерверные платформы (FAAS) могут быть неудобны для долгосрочных проектов?
57. Как выбрать между VPS и PAAS для высоконагруженного приложения? Какие критерии важны?
58. Какие преимущества дает использование AWS Elastic Beanstalk по сравнению с обычным VPS?
59. Как обеспечить безопасность при использовании локального туннеля (ngrok) в продакшене?
60. Опишите шаги для развертывания приложения на Heroku через CLI.
61. Как настроить HTTPS для пользовательского домена на платформе вроде Glitch?
62. Как запустить Node.js в режиме REPL?
63. Для чего используется переменная `_` в REPL?
64. Какая команда в REPL позволяет сохранить сессию в файл?
65. Как исследовать методы глобального объекта `Buffer` через REPL?
66. Чем отличается выполнение кода через `.editor` от ввода многострочного выражения без него?
67. Как прервать выполнение многострочного выражения в REPL?
68. Какие ограничения есть у REPL для отладки сложных приложений?
69. Как загрузить внешний модуль (например, `lodash`) в текущую сессию REPL?
70. Напишите в REPL цикл, который выводит числа от 1 до 5, используя `_` для доступа к предыдущему результату.
71. Как получить аргументы командной строки в Node.js-скрипте?
72. Что хранится в `process.argv[0]` и `process.argv[1]`?
73. Зачем использовать библиотеку `minimist` для обработки аргументов?
74. Как обработать аргумент вида `--port=3000` без сторонних библиотек?
75. Какие проблемы могут возникнуть при передаче аргументов в формате ключ-значение без валидации?
76. Как организовать обязательную проверку наличия определенного аргумента (например, `--config`)?
77. Напишите скрипт, который принимает аргумент `--env=production` и выводит соответствующее сообщение.
78. Чем отличается `console.error` от `console.log`?
79. Как вывести объект в консоль в формате JSON с помощью `console.log`?
80. Как измерить время выполнения функции с помощью `console.time`?
81. Зачем использовать библиотеку `chalk` вместо escape-последовательностей для цветов?
82. Почему `console.trace()` полезен для отладки асинхронного кода?
83. Как создать кастомный логгер с разделением выводов в `stdout` и `stderr`?
84. Реализуйте прогресс-бар для 10 итераций с задержкой 200 мс, используя библиотеку `progress`.
85. Какой модуль Node.js используется для чтения ввода из командной строки?
86. Чем отличается `readline-sync` от стандартного `readline`?
87. Зачем использовать библиотеку `inquirer` вместо `readline`?
88. Как безопасно запросить пароль без отображения вводимых символов?
89. Какие проблемы могут возникнуть при использовании `process.stdin` без обработки потоков?
90. Как реализовать меню с выбором вариантов (radio buttons) через `inquirer`?
91. Напишите скрипт, который спрашивает имя пользователя и выводит приветствие с задержкой 1 секунду.
92. Чем отличается `exports` от `module.exports`?
93. Как импортировать модуль из соседнего файла?
94. Почему нельзя перезаписать `exports` напрямую (например, `exports = {...}`)?
95. Как избежать циклических зависимостей между модулями?
96. Какие механизмы кеширования модулей есть в Node.js?
97. Как загрузить модуль из строки (динамически) без файла?
98. Создайте модуль `math.js`, экспортирующий функции `add` и `multiply`, и используйте его в `app.js`.
99. Какие инструменты хостинга вы бы выбрали для MVP и для высоконагруженного SaaS? Почему?
100. Опишите жизненный цикл Node.js-приложения от разработки до продакшена с использованием PAAS.
101. **Что такое npm и для чего он используется?**
102. **Как установить все зависимости проекта из `package.json`?**
103. **Чем отличается `npm install lodash` от `npm install -g lodash`?**
104. **Что делает флаг `--save-dev` при установке пакета?**
105. **Как запустить скрипт `start` из `package.json`?**
106. **Что такое `node_modules` и зачем она нужна?**
107. **Как добавить пакет в `dependencies` без ручного редактирования `package.json`?**
108. **Объясните разницу между `dependencies` и `devDependencies`.**  
109. **Что такое семантическое версионирование (semver)?**  
110. **Зачем нужен `package-lock.json`? Почему его нельзя игнорировать в Git?**
111. **Как установить конкретную версию пакета (например, React 16.8.1)?**
112. **Что делает команда `npm update`? Как обновить только один пакет?**
113. **Как указать, что проект требует Node.js версии 14 и выше?**
114. **Как запустить бинарный файл пакета (например, `webpack`), установленного локально?**  
115. **Какие проблемы решает `package-lock.json` и как он взаимодействует с `package.json`?**  _
116. **Как работает алгоритм разрешения зависимостей npm?**  
117. **Как оптимизировать размер `node_modules` в production?**  
118. **Что такое `peerDependencies` и в каких сценариях они используются?**  
    _Пример: Зачем библиотеке React указывать React как peer dependency?_
119. **Как настроить `npm scripts` для многоэтапной сборки (dev, build, test)?**  
120. **Как избежать проблем с глобальными пакетами при работе в команде?**  
121. **Как работает `engines` в `package.json`? Что произойдет, если версия Node.js не соответствует указанной?**
122. **Ситуация**: После `npm install` в проекте появились ошибки. Как быстро откатить зависимости к версиям из `package-lock.json`?  
123. **Объясните**: Почему в `package-lock.json` могут быть несколько версий одного пакета?  
124. **Как `npx` избегает глобальной установки пакетов?**
125. **Что делает `browserslist` в `package.json` и какие инструменты его используют?**
126. **Как `npm` обрабатывает конфликты зависимостей при использовании `npm link`?**
127. Что такое npm, и какие основные задачи он решает?
128. В чем разница между локальной и глобальной установкой npm-пакетов?
129. Как узнать, какие пакеты установлены в проекте?
130. Чем отличается `npm list` от `npm list --depth=0`?
131. Как просмотреть список глобально установленных пакетов?
132. Что такое package.json и package-lock.json?
133. Как узнать версию конкретного установленного пакета?
134. Как узнать самую последнюю версию пакета, доступную в npm-репозитории?
135. Как можно установить конкретную версию пакета?
136. Как посмотреть все доступные версии npm-пакета?
137. Как обновить все зависимости в проекте до их последних версий?
138. В чем разница между `npm update` и `npx npm-check-updates -u`?
139. Как удалить установленный npm-пакет?
140. В чем разница между `npm uninstall <package>` и `npm uninstall -g <package>`?
141. Как удалить глобально установленный npm-пакет?
142. Как проверить, какие пакеты в проекте устарели?
143. Почему npm не обновляет зависимости до мажорных версий при запуске `npm update`?
144. Чем отличаются `dependencies` и `devDependencies`?
145. Как установить пакет как зависимость разработки?
146. Как установить зависимости без зависимостей для разработки?
147. Почему для продакшн-среды стоит использовать `npm install --production`?
148. Что такое npx, и чем он полезен?
149. Как с помощью npx выполнить утилиту без её установки?
150. Как запустить скрипт с GitHub с помощью npx?
151. В чем разница между `npx create-react-app` и глобальной установкой `create-react-app`?
152. Как с помощью npx запустить код с разными версиями Node.js?
153. Что такое цикл событий (Event Loop) в JavaScript, и зачем он нужен?
154. Почему JavaScript является однопоточным, и какие преимущества это даёт?
155. Какие проблемы могут возникнуть при блокировке цикла событий?
156. Как работает стек вызовов (Call Stack) в JavaScript?
157. Что произойдёт, если в коде есть бесконечный цикл?
158. Как работает очередь событий (Event Queue), и как она связана с циклом событий?
159. В чём разница между `setTimeout(fn, 0)` и `setImmediate(fn)`?
160. Как работает `setTimeout()` и почему указанное время задержки не всегда соблюдается точно?
161. В каких случаях `setInterval()` может приводить к неожиданному поведению?
162. Как избежать накладок (`overlapping`) при использовании `setInterval()`?
163. Что такое очередь заданий (Job Queue) и чем она отличается от очереди событий?
164. Почему колбэк промиса (`.then()`) выполняется раньше, чем обработчик `setTimeout(fn, 0)`?
165. Что делает `process.nextTick()` в Node.js?
166. Когда лучше использовать `process.nextTick()`, а когда `setImmediate()`?
167. Может ли `process.nextTick()` заблокировать выполнение кода?
168. Как можно разбить тяжёлую синхронную задачу, чтобы она не блокировала цикл событий?
169. Что произойдёт, если в `setTimeout()` передать отрицательное значение задержки?
170. Как реализовать периодическую задачу без `setInterval()`, чтобы избежать наложения вызовов?
171. В каких случаях `setImmediate()` может выполняться раньше, чем `setTimeout(fn, 0)`?
172. Что такое асинхронность в JavaScript, и почему она важна?
173. Какие есть способы работы с асинхронным кодом в JavaScript?
174. В чем разница между синхронным и асинхронным кодом?
175. Что такое callback-функции, и как они работают?
176. Какие проблемы связаны с использованием callback-функций?
177. Как можно избежать callback hell?
178. Что такое промисы в JavaScript?
179. Какие состояния бывают у промиса?
180. В чем разница между `resolve` и `reject`?
181. Что делает метод `.then()`, `.catch()` и `.finally()`?
182. Как можно цепочкой обработать несколько асинхронных операций
183. Как работает `async/await`?
184. В чем преимущества `async/await` перед промисами?
185. Как правильно обрабатывать ошибки при использовании `async/await`?
186. Как работает Event Loop в JavaScript?
187. В чем разница между microtasks и macrotasks?
188. Как `setTimeout` и `setImmediate` взаимодействуют с Event Loop?
189. Как Node.js обрабатывает асинхронные операции?
190. В чем разница между асинхронным и многопоточным выполнением?
191. Как `fs.promises` отличается от `fs` с колбэками?
192. Что происходит, когда пользователь вводит URL в адресную строку браузера и нажимает Enter?
193. Что такое DNS и какую роль он играет в процессе выполнения HTTP-запроса?
194. Как браузер определяет IP-адрес домена? Как работает кэширование DNS?
195. В чем разница между протоколами TCP и UDP? Почему DNS-запросы выполняются через UDP?
196. Как работает установление TCP-соединения (трехфазное рукопожатие)?
197. Какие основные части содержит HTTP-запрос?
198. Какие бывают методы HTTP-запросов и чем они отличаются?
199. Какие коды состояния HTTP вы знаете? Приведите примеры и объясните их назначение.
200. Чем отличаются HTTP/1.1, HTTP/2 и HTTP/3?
201. Что такое заголовки HTTP-запроса? Какие заголовки чаще всего используются?
202. Какие модули в Node.js используются для выполнения HTTP-запросов?
203. Как выполнить HTTP GET-запрос в Node.js с использованием встроенного модуля `https`?
204. Как выполнить POST-запрос в Node.js с использованием модуля `https`?
205. Как можно обработать ошибки при выполнении HTTP-запросов в Node.js?
206. Чем удобен `axios` по сравнению с встроенным модулем `https`?
207. Как выполнить GET-запрос с параметрами, используя `axios`?
208. Как прервать HTTP-запрос в `axios`?
209. Какие преимущества у `axios` перед `fetch`?
210. Какой заголовок HTTP-запроса отвечает за передачу токена авторизации?
211. Как реализовать обработку ответа сервера в `axios`?
212. Чем WebSocket отличается от HTTP?
213. Какие преимущества дает WebSocket по сравнению с REST API?
214. Как создать WebSocket-соединение в Node.js?
215. Какие события существуют у WebSocket-соединения и для чего они используются?
216. Чем отличается `ws://` от `wss://`?
217. Как сервер может отправлять сообщения клиенту в WebSocket?
218. Какие библиотеки используются для работы с WebSocket в Node.js?
219. Как обработать ошибки при работе с WebSocket?
220. Какие задачи хорошо решаются с использованием WebSocket?
221. Как организовать авторизацию пользователя при работе с WebSocket?
222. Что такое файловый дескриптор в Node.js, и зачем он нужен?
223. Как открыть файл в Node.js с помощью `fs.open()`? Какие параметры принимает этот метод?
224. В чем разница между `fs.open()` и `fs.openSync()`?
225. Какие флаги можно использовать при открытии файла и что они означают (`r`, `r+`, `w+`, `a`, `a+` и т. д.)?
226. Какие ошибки могут возникнуть при открытии файла и как их обрабатывать?
227. Как получить информацию о файле в Node.js? Какие методы для этого существуют?
228. Какие свойства можно получить из объекта `stats`?
229. Как определить, является ли путь файлом, директорией или символической ссылкой?
230. В чем разница между `fs.stat()`, `fs.lstat()` и `fs.fstat()`?
231. Как работает модуль `path` в Node.js? Почему он полезен?
232. Как получить директорию, имя файла и расширение из пути (`path.dirname()`, `path.basename()`, `path.extname()`)?
233. В чем разница между `path.join()` и `path.resolve()`?
234. Что делает `path.normalize()`?
235. Как проверить, является ли путь абсолютным (`path.isAbsolute()`) и зачем это нужно?
236. Какие методы в `fs` используются для чтения файла?
237. В чем разница между `fs.readFile()` и `fs.readFileSync()`?
238. Как можно задать кодировку при чтении файла?
239. Чем потоковое чтение файлов лучше метода `fs.readFile()` при работе с большими файлами?
240. Как записать данные в файл? Какие флаги можно использовать для управления режимами записи (`fs.writeFile()`)?
241. Как дописать данные в файл (`fs.appendFile()`, `fs.appendFileSync()`)?
242. Как проверить, существует ли директория (`fs.access()`, `fs.existsSync()`) и какие проблемы могут возникнуть?
243. Как создать директорию? В чем разница между `fs.mkdir()` и `fs.mkdirSync()`?
244. Как прочитать содержимое директории (`fs.readdir()`, `fs.readdirSync()`)?
245. Как удалить директорию? Почему `fs.rmdir()` не удаляет непустые папки, и как это можно обойти?
246. Как рекурсивно удалить директорию в Node.js? Какие альтернативные пакеты можно использовать?
247. Чем `fs.promises` отличается от стандартных методов `fs`?
248. В чем разница между `fs.watchFile()` и `fs.watch()`?
249. Как работает `fs.createReadStream()` и `fs.createWriteStream()`, и зачем их использовать?
250. Какие методы модуля `fs` блокируют главный поток? Почему стоит избегать их в асинхронных приложениях?
251. Как можно безопасно работать с файлами в многопоточной среде (например, при одновременной записи в файл несколькими процессами)?
252. Как получить информацию о процессорах системы в Node.js?
253. Что возвращает метод `os.endianness()` и зачем это нужно?
254. Как определить домашнюю директорию текущего пользователя с помощью Node.js?**
255. Чем отличаются значения `os.EOL` в Windows и POSIX-системах? Приведите примеры использования.
256. Как получить список сигналов процесса, доступных в ОС, через Node.js? Для чего это может пригодиться?
257. Какие данные содержит объект, возвращаемый `os.networkInterfaces()`? Приведите пример структуры.
258. Напишите код, который выводит общий объем памяти и свободную память в гигабайтах.
259. Как определить, работает ли приложение на Windows, используя модуль `os`?
260. Что такое `EventEmitter`? Как создать его экземпляр?
261. Чем отличаются методы `emitter.on()` и `emitter.once()`?
262. Как удалить все обработчики для события `open` из `EventEmitter`
263. Что такое события `newListener` и `removeListener`? Приведите пример их использования.
264. Как ограничить максимальное количество обработчиков для события в `EventEmitter`?
265. Зачем нужны методы `prependListener()` и `prependOnceListener()`? В чем их отличие от `on()`?
266. Создайте `EventEmitter`, который генерирует событие `data` каждую секунду, и обработайте его.
267. Напишите код, который подсчитывает количество обработчиков для события `error`.
268. Как создать HTTP-сервер в Node.js? Приведите пример кода.
269. Чем отличаются методы `http.get()` и `http.request()`?
270. Какие HTTP-статусы соответствуют кодам 404, 500, 200?
271. Что такое `http.Agent` и как он управляет соединениями?
272. Как получить HTTP-метод (GET, POST) из входящего запроса?
273. Какие данные доступны в объекте `http.IncomingMessage`?
274. Напишите сервер, который возвращает текущее время при GET-запросе на `/time`.
275. Как обработать POST-запрос и прочитать тело запроса?
276. Какие типы потоков существуют в Node.js? Чем они отличаются?
277. Что делает метод `pipe()`? Приведите пример использования.
278. Какие преимущества дают потоки при работе с большими файлами?
279. Как создать кастомный `Transform`-поток, который преобразует данные в верхний регистр
280. Что произойдет, если в `Writable`-потоке не вызвать `next()` в методе `_write()`?
281. Как обработать ошибку в потоке?
282. Напишите код, который читает файл и отправляет его содержимое в ответ HTTP-сервера через поток.
283. Создайте дуплексный поток, который логирует все полученные данные.
284. Как установить соединение с MySQL в Node.js? Какие параметры обязательны?
285. Как выполнить запрос `SELECT * FROM users WHERE id = ?` с параметром?
286. Как безопасно закрыть соединение с базой данных?
287. Как обработать SQL-инъекции при использовании пакета `mysqljs/mysql`?
288. Что возвращает метод `connection.query()` при вставке данных (INSERT)?
289. Как настроить пул соединений с MySQL?
290. Напишите код, который добавляет новую запись в таблицу `todos` и возвращает её ID.
291. Реализуйте обработку ошибки при подключении к несуществующей базе данных.
292. Как установить переменную окружения `NODE_ENV` в production?
293. Какие оптимизации обычно применяются в продакшн-среде для Express?
294. Зачем нужен `express.errorHandler` и как он настраивается для разных сред?
295. Как поведение кэширования шаблонов в Express зависит от `NODE_ENV`?
296. Почему в продакшне рекомендуется минимизировать логирование?
297. Как использовать разные конфигурации для staging и production?
298. Почему важно обрабатывать ошибки в колбэках потоков?
299. Как избежать утечек памяти при работе с `EventEmitter`?**
300. Как потоки помогают снизить нагрузку на память?
301. Какие методы модуля `os` могут помочь в мониторинге производительности?