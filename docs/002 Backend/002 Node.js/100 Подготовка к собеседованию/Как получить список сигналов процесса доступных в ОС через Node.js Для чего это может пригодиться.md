---
title: Как получить список сигналов процесса доступных в ОС через Node.js Для чего это может пригодиться
draft: false
tags:
  - "#NodeJS"
  - "#os"
  - "#process"
  - "#сигналы"
  - "#constants"
  - "#системное-программирование"
info:
  - "[Документация Node.js по модулю os](https://nodejs.org/api/os.html#os_os_constants_signals)"
  - "[Документация Node.js по обработке сигналов](https://nodejs.org/api/process.html#process_signal_events)"
  - "[Сигналы процессов в POSIX системах](https://en.wikipedia.org/wiki/Signal_(IPC))"
---

Для получения списка сигналов, доступных в операционной системе, используется **`os.constants.signals`**.

### **1. Получение списка доступных сигналов**

```javascript
const os = require("os")

console.log("Список доступных сигналов:", os.constants.signals)
```

📌 **Вывод на Linux/macOS:**

```json
{
  "SIGHUP": 1,
  "SIGINT": 2,
  "SIGQUIT": 3,
  "SIGILL": 4,
  "SIGTRAP": 5,
  "SIGABRT": 6,
  "SIGBUS": 7,
  "SIGFPE": 8,
  "SIGKILL": 9,
  "SIGUSR1": 10,
  "SIGSEGV": 11,
  "SIGUSR2": 12,
  "SIGPIPE": 13,
  "SIGALRM": 14,
  "SIGTERM": 15
}
```

📌 **Вывод на Windows:**

```json
{
  "SIGINT": 2,
  "SIGBREAK": 21,
  "SIGTERM": 15
}
```

📌 **На Windows поддерживается ограниченное количество сигналов**.

### **2. Для чего это может пригодиться?**

#### **2.1. Управление процессами (завершение, перезапуск)**

Пример обработки сигнала **SIGINT** (Ctrl + C в консоли):

```javascript
process.on("SIGINT", () => {
  console.log("Процесс завершается...")
  process.exit(0)
})
```

#### **2.2. Корректное завершение фоновых сервисов**

Можно перехватывать **SIGTERM** (например, при остановке Docker-контейнера):

```javascript
process.on("SIGTERM", () => {
  console.log("Сервис завершает работу...")
  // Очистка ресурсов перед завершением
  process.exit(0)
})
```

#### **2.3. Отправка сигналов другим процессам**

```javascript
process.kill(pid, "SIGTERM") // Отправляет процессу с `pid` сигнал завершения
```

### **Вывод**

- **`os.constants.signals`** содержит список доступных сигналов ОС.
- Используется для **управления процессами**, **обработки завершения** и **межпроцессного взаимодействия**.
- На **Windows поддерживается меньше сигналов**, чем на **Linux/macOS**.

---

[[002 Node.js|Назад]]
