---
title: Чем отличаются значения os.EOL в Windows и POSIX-системах Приведите примеры использования.
draft: false
tags:
  - "#NodeJS"
  - "#os"
  - "#EOL"
  - "#Windows"
  - "#POSIX"
  - "#кроссплатформенность"
info:
  - "[Документация по модулю os в Node.js](https://nodejs.org/api/os.html#oseol)"
  - "[Руководство по файловым системам в Node.js](https://nodejs.org/api/fs.html)"
  - "[Различия операционных систем в контексте Node.js](https://nodejs.org/api/cli.html#differences-between-windows-and-posix)"
---

Переменная **`os.EOL`** в Node.js содержит символ(ы) конца строки (End Of Line) для текущей операционной системы.

#### **1. Различия между Windows и POSIX (Linux/macOS)**

| Операционная система | Значение `os.EOL` | Кодировка                   |
| -------------------- | ----------------- | --------------------------- |
| **Windows**          | `\r\n` (CRLF)     | Carriage Return + Line Feed |
| **Linux/macOS**      | `\n` (LF)         | Line Feed                   |

📌 **Пример использования `os.EOL`**

```javascript
const os = require("os")

console.log("Конец строки в этой ОС:", JSON.stringify(os.EOL))
```

✅ **Вывод на Windows:**

```bash
"\r\n"
```

✅ **Вывод на Linux/macOS:**

```bash
"\n"
```

---

### **2. Примеры использования**

#### **2.1. Запись текста в файл с корректными переводами строк**

```javascript
const fs = require("fs")
const os = require("os")

const text = ["Первая строка", "Вторая строка", "Третья строка"].join(os.EOL)

fs.writeFileSync("output.txt", text)
console.log("Файл записан с корректными переводами строк.")
```

📌 Этот код создаст `output.txt` с правильными переводами строк для текущей ОС.

---

#### **2.2. Разбиение строки по строкам в файле**

```javascript
const fs = require("fs")
const os = require("os")

const content = fs.readFileSync("output.txt", "utf-8")
const lines = content.split(os.EOL)

console.log("Строки в файле:", lines)
```

📌 Этот код корректно разобьёт текст по строкам независимо от ОС.

---

### **Вывод**

- **Windows использует `\r\n`, POSIX (Linux/macOS) — `\n`**.
- **`os.EOL` позволяет писать кроссплатформенный код**, обеспечивая совместимость при работе с файлами и строками.

---

[[002 Node.js|Назад]]
