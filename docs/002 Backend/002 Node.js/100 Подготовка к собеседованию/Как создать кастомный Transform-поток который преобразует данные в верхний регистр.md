---
title: Как создать кастомный Transform-поток который преобразует данные в верхний регистр
draft: false
tags:
  - "#NodeJS"
  - "#stream"
  - "#Transform"
  - "#потоки-данных"
  - "#pipe"
info:
  - "[Документация Node.js по Streams](https://nodejs.org/api/stream.html)"
  - "[Документация по классу Transform](https://nodejs.org/api/stream.html#class-streamtransform)"
---

В Node.js модуль `stream` предоставляет класс **`Transform`**, позволяющий создавать потоки с изменением данных "на лету".

## 1. Код кастомного Transform-потока

```javascript
const { Transform } = require("stream")

class UpperCaseTransform extends Transform {
  _transform(chunk, encoding, callback) {
    const upperCaseData = chunk.toString().toUpperCase() // Преобразуем в верхний регистр
    this.push(upperCaseData) // Передаём обработанные данные дальше
    callback()
  }
}

// Экспортируем поток (если нужно)
module.exports = UpperCaseTransform
```

📌 **Как это работает?**

- Метод `_transform(chunk, encoding, callback)` получает входные данные (`chunk`).
- `chunk.toString().toUpperCase()` — преобразуем в верхний регистр.
- `this.push(upperCaseData)` — передаём обработанный результат.
- `callback()` — сигнализируем, что обработка завершена.

## 2. Пример использования (чтение из файла → преобразование → запись в другой файл)

```javascript
const fs = require("fs")
const UpperCaseTransform = require("./UpperCaseTransform")

const readStream = fs.createReadStream("input.txt") // Файл с текстом
const writeStream = fs.createWriteStream("output.txt") // Файл для результата
const upperCaseTransform = new UpperCaseTransform()

readStream.pipe(upperCaseTransform).pipe(writeStream)

writeStream.on("finish", () => console.log("Файл успешно обработан."))
```

📌 **Что делает код?**

1. Читает файл `input.txt` через `fs.createReadStream()`.
2. Пропускает данные через `UpperCaseTransform`, преобразуя в **верхний регистр**.
3. Записывает результат в `output.txt`.
4. По завершению выводит сообщение `"Файл успешно обработан."`.

## 3. Пример работы с консолью (stdin → stdout)

```javascript
process.stdin.pipe(new UpperCaseTransform()).pipe(process.stdout)
```

📌 **Что делает код?**

- Вводимые в терминале строки сразу выводятся в верхнем регистре.
- Можно протестировать, просто запустив и набирая текст в консоли.

## 4. Дополнительные возможности Transform-потоков

```javascript
const { Transform } = require("stream")

// Создание трансформирующего потока с опциями
const upperCaseStream = new Transform({
  transform(chunk, encoding, callback) {
    // Обработка ошибок
    try {
      const result = chunk.toString().toUpperCase()
      callback(null, result)
    } catch (err) {
      callback(err)
    }
  },
  // Другие опции
  objectMode: false, // true для работы с объектами вместо буферов
  highWaterMark: 16384, // размер внутреннего буфера
})
```

## Вывод

✅ **`Transform`-потоки позволяют обрабатывать данные на лету**.  
✅ **Кастомный `UpperCaseTransform` преобразует текст в верхний регистр**.  
✅ **Может использоваться с файлами, HTTP-серверами и потоками ввода-вывода**.
✅ **Позволяет эффективно обрабатывать большие объемы данных без загрузки всего в память**.

---

[[002 Node.js|Назад]]
