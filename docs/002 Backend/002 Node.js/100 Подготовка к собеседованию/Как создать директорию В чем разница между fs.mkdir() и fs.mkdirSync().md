---
title: Как создать директорию В чем разница между fs.mkdir() и fs.mkdirSync()
draft: true
tags: NodeJS
info:
---
### 🔹 Как создать директорию в Node.js?

Для создания директории (папки) в Node.js используются два метода:

- **Асинхронный**: `fs.mkdir()`
- **Синхронный**: `fs.mkdirSync()`

---

## ✅ 1. Асинхронный метод: `fs.mkdir()`

**Рекомендуется** для асинхронных приложений, так как не блокирует поток выполнения.

js

КопироватьРедактировать

`const fs = require('fs');  fs.mkdir('myFolder', { recursive: true }, (err) => {   if (err) {     console.error('Ошибка при создании директории:', err);   } else {     console.log('Директория успешно создана');   } });`

### 📌 Разбор кода:

- `recursive: true` – позволяет создать **вложенные директории** (например, `parent/child`).
- Если директория **уже существует**, ошибка **не возникает**.
- Ошибки (например, недостаточно прав) обрабатываются в `err`.

**➕ Плюсы**:

- Не блокирует выполнение кода (лучше для серверных приложений).
- Поддерживает `recursive: true` (создает родительские папки автоматически).

**➖ Минусы**:

- Требует обработку ошибок через callback.

---

## ✅ 2. Синхронный метод: `fs.mkdirSync()`

Работает аналогично, но **блокирует выполнение кода** до завершения операции.

js

КопироватьРедактировать

`const fs = require('fs');  try {   fs.mkdirSync('myFolder', { recursive: true });   console.log('Директория успешно создана'); } catch (err) {   console.error('Ошибка при создании директории:', err); }`

**➕ Плюсы**:

- Код проще, так как можно использовать `try...catch`.
- Подходит для **скриптов CLI** или однократных операций.

**➖ Минусы**:

- Блокирует выполнение (нежелательно в асинхронных приложениях).

---

## ❗ Разница между `fs.mkdir()` и `fs.mkdirSync()`

|Метод|Асинхронность|Блокирует выполнение?|Использование|
|---|---|---|---|
|`fs.mkdir()`|✅ Да|❌ Нет|Серверные приложения|
|`fs.mkdirSync()`|❌ Нет|✅ Да|CLI-скрипты, тесты|

---

## 🔥 Итог:

- **Используйте `fs.mkdir()`**, если приложение **асинхронное** и важна производительность.
- **Используйте `fs.mkdirSync()`**, если код выполняется **единоразово** и блокировка не критична.
- **Всегда указывайте `{ recursive: true }`**, если папка может существовать или нужно создать вложенные директории. 🚀