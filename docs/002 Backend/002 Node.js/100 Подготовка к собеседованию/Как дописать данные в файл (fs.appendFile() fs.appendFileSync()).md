---
title: Как дописать данные в файл (fs.appendFile() fs.appendFileSync())
draft: true
tags: NodeJS
info:
---
### 🔹 Как дописать данные в файл в Node.js?

Чтобы **добавить данные** в конец файла (а не перезаписать его), используйте `fs.appendFile()` или `fs.appendFileSync()`.

---

### 🔹 1. Асинхронный способ: `fs.appendFile()`

Метод `fs.appendFile()` добавляет данные в файл **асинхронно**.  
📌 **Если файла нет**, он **создаст его автоматически**.

js

КопироватьРедактировать

`const fs = require('fs');  fs.appendFile('log.txt', 'Новая строка\n', (err) => {   if (err) {     console.error('Ошибка при записи:', err);     return;   }   console.log('Данные добавлены в файл'); });`

📌 **Важно:** Этот метод **не блокирует** выполнение кода, что делает его хорошим выбором для асинхронных приложений.

---

### 🔹 2. Синхронный способ: `fs.appendFileSync()`

Если вам нужно записывать данные **блокирующим** способом (например, перед завершением программы), используйте `fs.appendFileSync()`:

js

КопироватьРедактировать

`fs.appendFileSync('log.txt', 'Синхронная запись\n'); console.log('Файл обновлен');`

⚠️ **Минус:** Этот метод **блокирует выполнение** всего кода, пока не завершится запись.

---

### 🔹 3. Управление кодировкой

По умолчанию Node.js записывает файлы в **байтах**. Чтобы задать **кодировку** (например, UTF-8), передайте её вторым аргументом:

js

КопироватьРедактировать

`fs.appendFile('log.txt', 'Добавлено с UTF-8\n', 'utf8', (err) => {   if (err) throw err;   console.log('Данные добавлены с кодировкой UTF-8'); });`

Для `fs.appendFileSync()`:

js

КопироватьРедактировать

`fs.appendFileSync('log.txt', 'Еще одна строка\n', 'utf8');`

---

### 🔹 Итог:

- `fs.appendFile()` — **асинхронное** добавление данных (не блокирует выполнение).
- `fs.appendFileSync()` — **синхронное** добавление (блокирует поток).
- Оба метода **создают файл**, если он не существует.
- Можно указывать **кодировку**, например `'utf8'`.

🚀 **Рекомендуется использовать `fs.appendFile()` для асинхронных приложений**, а `fs.appendFileSync()` — только когда важно, чтобы код выполнялся последовательно.