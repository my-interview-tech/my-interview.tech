---
title: Что возвращает метод os.endianness() и зачем это нужно
draft: true
tags: NodeJS
info:
---
Метод **`os.endianness()`** возвращает порядок байтов (эндианность) процессора системы, в которой выполняется Node.js.

---

### **1. Возможные значения**

Метод возвращает строку:

- **`'LE'`** (Little Endian) – младший байт хранится первым (наиболее распространённый вариант).
- **`'BE'`** (Big Endian) – старший байт хранится первым (реже встречается).

#### **Пример использования**

js

КопироватьРедактировать

`const os = require('os');  console.log('Эндианность системы:', os.endianness());`

📌 **Вывод (на большинстве систем)**

bash

КопироватьРедактировать

`Эндианность системы: LE`

---

### **2. Зачем это нужно?**

Эндианность важна при обработке бинарных данных, например:

1. **Работа с бинарными файлами**
    
    - Если файл создан на системе с Big Endian, а читается на Little Endian, байты могут быть перевёрнуты.
    - `os.endianness()` помогает правильно интерпретировать данные.
2. **Сетевые протоколы**
    
    - В сетевых стандартах (например, TCP/IP) используются **Big Endian** (Network Byte Order).
    - Если система работает в **Little Endian**, данные могут потребовать конвертации.
3. **Кроссплатформенная разработка**
    
    - При разработке низкоуровневых модулей Node.js (например, через `Buffer` или `ArrayBuffer`) важно учитывать порядок байтов.

#### **Пример: Корректное чтение 32-битного числа**

js

КопироватьРедактировать

`const buffer = Buffer.from([0x01, 0x00, 0x00, 0x00]);  if (os.endianness() === 'LE') {   console.log(buffer.readUInt32LE(0)); // 1 (Little Endian) } else {   console.log(buffer.readUInt32BE(0)); // 16777216 (Big Endian) }`

📌 **Если порядок байтов не учесть, можно получить неверные результаты.**

---

### **Вывод**

- **`os.endianness()`** возвращает `'LE'` или `'BE'` в зависимости от архитектуры процессора.
- Используется при работе с **бинарными файлами**, **сетевыми протоколами** и **низкоуровневыми операциями с памятью**.