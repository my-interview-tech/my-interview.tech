---
title: Какие данные содержит объект возвращаемый os.networkInterfaces() Приведите пример структуры.
draft: true
tags: NodeJS
info:
---
Метод **`os.networkInterfaces()`** возвращает информацию о сетевых интерфейсах текущей системы.

---

### **1. Формат возвращаемого объекта**

Этот метод возвращает объект, где **ключи** — имена сетевых интерфейсов (**eth0, lo, Wi-Fi** и т. д.), а **значения** — массивы с информацией о каждом IP-адресе интерфейса.

#### **Пример структуры данных (`console.log(os.networkInterfaces())`)**

json

КопироватьРедактировать

`{   "lo": [     {       "address": "127.0.0.1",       "netmask": "255.0.0.0",       "family": "IPv4",       "mac": "00:00:00:00:00:00",       "internal": true,       "cidr": "127.0.0.1/8"     },     {       "address": "::1",       "netmask": "ffff:ffff:ffff:ffff::",       "family": "IPv6",       "mac": "00:00:00:00:00:00",       "internal": true,       "cidr": "::1/128"     }   ],   "eth0": [     {       "address": "192.168.1.10",       "netmask": "255.255.255.0",       "family": "IPv4",       "mac": "a1:b2:c3:d4:e5:f6",       "internal": false,       "cidr": "192.168.1.10/24"     }   ],   "Wi-Fi": [     {       "address": "192.168.1.15",       "netmask": "255.255.255.0",       "family": "IPv4",       "mac": "aa:bb:cc:dd:ee:ff",       "internal": false,       "cidr": "192.168.1.15/24"     }   ] }`

---

### **2. Описание свойств**

Каждый объект интерфейса содержит следующие поля:

|Поле|Описание|
|---|---|
|`address`|IP-адрес интерфейса (IPv4 или IPv6).|
|`netmask`|Маска подсети для этого интерфейса.|
|`family`|Версия IP: `"IPv4"` или `"IPv6"`.|
|`mac`|MAC-адрес интерфейса.|
|`internal`|`true`, если это локальный (виртуальный) интерфейс (например, `lo`), иначе `false`.|
|`cidr`|IP-адрес в формате CIDR (`192.168.1.10/24`).|

---

### **3. Пример использования**

#### **Вывести список всех IPv4-адресов**

js

КопироватьРедактировать

``const os = require('os');  const interfaces = os.networkInterfaces();  for (const name in interfaces) {   for (const net of interfaces[name]) {     if (net.family === 'IPv4' && !net.internal) {       console.log(`Интерфейс: ${name}, IP: ${net.address}`);     }   } }``

📌 **Вывод (пример)**

bash

КопироватьРедактировать

`Интерфейс: eth0, IP: 192.168.1.10 Интерфейс: Wi-Fi, IP: 192.168.1.15`

---

### **Вывод**

- **`os.networkInterfaces()`** возвращает объект с данными о всех сетевых интерфейсах.
- **Содержит IP-адреса (IPv4, IPv6), MAC, маску сети и другие параметры.**
- **Можно использовать для получения локального IP, фильтрации внешних/внутренних интерфейсов.**