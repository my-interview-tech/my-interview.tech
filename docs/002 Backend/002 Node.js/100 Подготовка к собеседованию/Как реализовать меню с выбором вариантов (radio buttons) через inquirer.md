---
title: Как реализовать меню с выбором вариантов (radio buttons) через inquirer
draft: false
tags:
  - "#NodeJS"
  - "#inquirer"
  - "#CLI"
  - "#интерфейс"
  - "#пользовательский-ввод"
info:
  - "[Документация Inquirer.js](https://github.com/SBoudrias/Inquirer.js)"
  - "[NPM-пакет Inquirer](https://www.npmjs.com/package/inquirer)"
  - "[Создание CLI с помощью Node.js](https://nodejs.dev/learn/output-to-the-command-line-using-nodejs)"
---

Библиотека **`inquirer`** позволяет создавать интерактивные CLI (командной строки) с разными типами пользовательского ввода. Для реализации меню с выбором вариантов (аналог radio buttons) используется тип вопроса **`list`**, который позволяет пользователю выбрать один вариант из предложенного списка.

## Установка inquirer

Сначала установите пакет через npm:

```bash
npm install inquirer
```

## Базовый пример меню с выбором

```javascript
const inquirer = require("inquirer")

const menu = [
  {
    type: "list", // Тип вопроса - список
    name: "choice", // Имя ответа
    message: "Выберите вариант:", // Сообщение для пользователя
    choices: [
      "Опция 1", // Первый вариант
      "Опция 2", // Второй вариант
      "Опция 3", // Третий вариант
      new inquirer.Separator(), // Разделитель
      "Опция 4", // Четвёртый вариант
    ],
    filter: function (val) {
      // Дополнительная обработка ответа (опционально)
      return val
    },
  },
]

// Запрос меню
inquirer.prompt(menu).then((answers) => {
  console.log(`Вы выбрали: ${answers.choice}`)
})
```

## Пояснение параметров

- **`type: 'list'`** — используется для создания списка с возможностью выбора одного из элементов. Это аналог radio-кнопок в GUI-интерфейсах.
- **`name: 'choice'`** — имя для ответа, который будет доступен в объекте `answers`.
- **`message`** — сообщение, которое будет отображаться пользователю перед предложением вариантов.
- **`choices`** — массив доступных вариантов. Можно использовать строки или объекты для более гибкой настройки.
- **`filter`** — функция для дополнительной обработки выбранного значения перед сохранением в объекте ответов.

## Расширенный пример с форматированием

```javascript
const inquirer = require("inquirer")

inquirer
  .prompt([
    {
      type: "list",
      name: "theme",
      message: "Выберите тему оформления:",
      choices: [
        {
          name: "🌞 Светлая тема",
          value: "light",
        },
        {
          name: "🌙 Тёмная тема",
          value: "dark",
        },
        {
          name: "🎨 Цветная тема",
          value: "color",
        },
        new inquirer.Separator("--- Системные опции ---"),
        {
          name: "⚙️ Автоматически (по системным настройкам)",
          value: "auto",
        },
      ],
      default: "light", // Значение по умолчанию
    },
    {
      type: "list",
      name: "fontSize",
      message: "Выберите размер шрифта:",
      choices: ["Маленький", "Средний", "Большой"],
      default: "Средний",
      when: (answers) => answers.theme !== "auto", // Условное отображение
    },
  ])
  .then((answers) => {
    console.log("Выбранные настройки:")
    console.log(JSON.stringify(answers, null, 2))
  })
  .catch((error) => {
    console.error("Произошла ошибка:", error)
  })
```

## Пример вывода в консоли

При запуске примера выше в терминале пользователь увидит:

```
? Выберите вариант: (Use arrow keys)
❯ Опция 1
  Опция 2
  Опция 3
  --------
  Опция 4
```

После выбора с помощью стрелок и нажатия Enter, будет выведен результат:

```
? Выберите вариант: Опция 2
Вы выбрали: Опция 2
```

## Дополнительные возможности

### Объекты вместо строк для более гибких опций

```javascript
const choices = [
  {
    name: "JavaScript", // Текст, отображаемый пользователю
    value: "js", // Значение, которое будет сохранено
    short: "JS", // Короткое имя для отображения в результатах
  },
  {
    name: "TypeScript",
    value: "ts",
    short: "TS",
  },
]
```

### Динамические варианты на основе предыдущих ответов

```javascript
const inquirer = require("inquirer")

inquirer
  .prompt([
    {
      type: "list",
      name: "language",
      message: "Выберите язык программирования:",
      choices: ["JavaScript", "Python", "Ruby"],
    },
    {
      type: "list",
      name: "framework",
      message: "Выберите фреймворк:",
      choices: (answers) => {
        // Выбор фреймворков на основе выбранного языка
        if (answers.language === "JavaScript") {
          return ["React", "Vue", "Angular"]
        } else if (answers.language === "Python") {
          return ["Django", "Flask", "FastAPI"]
        } else {
          return ["Rails", "Sinatra"]
        }
      },
    },
  ])
  .then((answers) => {
    console.log(`Выбрано: ${answers.language} + ${answers.framework}`)
  })
```

## Заключение

Библиотека inquirer предоставляет простой и мощный способ создания интерактивных CLI-интерфейсов в Node.js. Тип вопроса `list` эффективно имитирует радио-кнопки в терминале, позволяя пользователю выбрать один вариант из списка доступных опций.

Для более сложных сценариев inquirer предлагает и другие типы вопросов:

- `checkbox` — для выбора нескольких вариантов
- `confirm` — для да/нет вопросов
- `input` — для свободного ввода текста
- `password` — для ввода паролей
- `editor` — для многострочного ввода

---

[[002 Node.js|Назад]]
