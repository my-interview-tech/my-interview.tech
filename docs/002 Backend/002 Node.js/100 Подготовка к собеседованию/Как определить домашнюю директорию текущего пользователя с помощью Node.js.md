---
title: Как определить домашнюю директорию текущего пользователя с помощью Node.js
draft: true
tags: NodeJS
info:
---
В Node.js для получения пути к домашней директории используются методы модуля **`os`** и переменная окружения **`HOME`**/**`USERPROFILE`**.

---

### **1. Метод `os.homedir()` (Рекомендуемый способ)**

Метод **`os.homedir()`** возвращает путь к домашней папке текущего пользователя.

#### **Пример использования**

js

КопироватьРедактировать

`const os = require('os');  console.log('Домашняя директория:', os.homedir());`

📌 **Вывод на Windows:**

bash

КопироватьРедактировать

`C:\Users\ИмяПользователя`

📌 **Вывод на Linux/macOS:**

bash

КопироватьРедактировать

`/home/username`

**✅ Подходит для всех операционных систем**.

---

### **2. Использование переменных окружения (`process.env`)**

На разных ОС путь к домашней директории хранится в разных переменных окружения:

- **Windows:** `process.env.USERPROFILE`
- **Linux/macOS:** `process.env.HOME`

#### **Пример: универсальное получение пути через `process.env`**

js

КопироватьРедактировать

`const homeDir = process.env.HOME || process.env.USERPROFILE; console.log('Домашняя директория:', homeDir);`

📌 **Этот метод менее предпочтителен, так как `os.homedir()` учитывает нюансы системы.**

---

### **Вывод**

✅ **Лучший способ** – `os.homedir()`, так как он кроссплатформенный.  
✅ **Можно использовать `process.env`, но он менее надёжен.**