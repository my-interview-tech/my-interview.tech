---
title: Какая команда в REPL позволяет сохранить сессию в файл
draft: false
tags:
  - "#NodeJS"
  - "#REPL"
  - "#командная-строка"
  - "#JavaScript"
info:
  - "[Документация Node.js REPL](https://nodejs.org/api/repl.html)"
  - "[Учебное пособие по Node.js REPL](https://nodejs.org/en/learn/command-line/how-to-use-the-nodejs-repl)"
---

В **Node.js REPL** команда **`.save`** позволяет сохранить текущую сессию в файл.

### Синтаксис:

```bash
.save <путь к файлу>
```

### Пример использования:

1. Откройте REPL с помощью команды:

   ```bash
   node
   ```

2. Введите какие-либо команды в REPL:

   ```javascript
   > const a = 10;
   > const b = 20;
   > a + b
   30
   ```

3. Чтобы сохранить текущую сессию в файл (например, в файл `session.js`), выполните команду:

   ```bash
   .save session.js
   ```

4. В результате файл `session.js` будет содержать все введенные в REPL команды:

   ```javascript
   const a = 10
   const b = 20
   a + b
   ```

### Другие полезные команды REPL:

- **`.help`** - показывает список всех доступных команд
- **`.load <файл>`** - загружает JavaScript-файл в текущую сессию REPL
- **`.editor`** - включает режим редактора для написания многострочного кода
- **`.clear`** - очищает контекст REPL, удаляя все объявленные переменные
- **`.exit`** или **`.quit`** - выход из REPL

### Примечания:

- Файл будет создан в текущей рабочей директории, если путь к файлу не указан полностью.
- Вы можете указать полный путь к файлу, чтобы сохранить сессию в нужное место.

Эта команда полезна, если вы хотите сохранить результаты работы с REPL и продолжить их использовать в будущем.

---

[[002 Node.js|Назад]]
