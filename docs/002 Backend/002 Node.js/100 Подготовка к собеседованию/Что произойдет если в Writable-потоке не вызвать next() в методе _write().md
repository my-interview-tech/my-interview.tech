---
title: Что произойдет если в Writable-потоке не вызвать next() в методе _write()
draft: true
tags: NodeJS
info:
---
Если в кастомном `Writable`-потоке в методе `_write()` не вызвать `next()`, то:

1. **Поток зависнет** и **перестанет обрабатывать новые данные**.
2. Метод `_write()` **не сообщит о завершении обработки** текущего чанка, и поток **останется в состоянии ожидания**.
3. **Другие операции с потоком (pipe, write)** не смогут продолжиться, так как поток будет **заблокирован**.

---

### **Пример проблемы (забыли `next()`)**

js

КопироватьРедактировать

`const { Writable } = require('stream');  class MyWritable extends Writable {   _write(chunk, encoding, next) {     console.log(chunk.toString()); // Выводим данные     // ❌ Ошибка: забыли вызвать next()   } }  const writable = new MyWritable();  writable.write('Hello'); // Выведет "Hello", но поток зависнет writable.write('World'); // Не дойдет до обработки`

📌 **Что произойдет?**

- Первый `write('Hello')` выполнится, но поток **не освободится для следующего чанка**, так как `next()` не вызван.
- Второй `write('World')` **никогда не будет обработан**, так как поток "завис".

---

### **Правильный код с `next()`**

js

КопироватьРедактировать

`class MyWritable extends Writable {   _write(chunk, encoding, next) {     console.log(chunk.toString());     next(); // ✅ Освобождаем поток для следующего чанка   } }`

📌 **Теперь поток работает корректно**, и каждый новый `write()` будет выполняться.

---

### **Вывод**

- В `Writable`-потоке **`next()` обязателен** для завершения обработки чанка.
- Без `next()` **поток зависнет** и **не примет новые данные**.
- Всегда вызывайте `next()`, даже если обработка синхронная.