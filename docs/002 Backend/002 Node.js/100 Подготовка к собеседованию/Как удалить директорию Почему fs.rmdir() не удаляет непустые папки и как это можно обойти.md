---
title: Как удалить директорию Почему fs.rmdir() не удаляет непустые папки и как это можно обойти
draft: true
tags: NodeJS
info:
---
### 🗑️ Как удалить директорию в Node.js?

В Node.js для удаления папок используются два метода:

- **Асинхронный:** `fs.rmdir()` (⚠️ устарел, использовать `fs.rm()`).
- **Синхронный:** `fs.rmdirSync()` (⚠️ устарел, использовать `fs.rmSync()`).

---

## ❌ Почему `fs.rmdir()` не удаляет непустые папки?

Метод `fs.rmdir()` **не может удалить** директорию, если в ней есть файлы или подпапки.  
Попытка удалить непустую папку вызовет ошибку:

js

КопироватьРедактировать

`const fs = require('fs');  fs.rmdir('./myFolder', (err) => {   if (err) {     console.error('Ошибка:', err); // Ошибка: ENOTEMPTY (директория не пуста)   } else {     console.log('Папка удалена');   } });`

**✅ Решение:** использовать `fs.rm()` с флагом `{ recursive: true }`.

---

## ✅ 1. Асинхронное удаление с `fs.rm()`

Метод `fs.rm()` позволяет удалить **непустые папки**.

js

КопироватьРедактировать

`fs.rm('./myFolder', { recursive: true, force: true }, (err) => {   if (err) {     console.error('Ошибка:', err);   } else {     console.log('Папка удалена');   } });`

### 📌 Разбор параметров:

- `{ recursive: true }` – удаляет папку и все вложенные файлы.
- `{ force: true }` – игнорирует ошибки, если папки нет.

---

## ✅ 2. Синхронное удаление с `fs.rmSync()`

Синхронный вариант подходит для CLI-скриптов.

js

КопироватьРедактировать

`const fs = require('fs');  try {   fs.rmSync('./myFolder', { recursive: true, force: true });   console.log('Папка удалена'); } catch (err) {   console.error('Ошибка:', err); }`

---

## ✅ 3. Рекурсивное удаление вручную (`fs.readdir() + fs.unlink()`)

Если нужно удалить файлы **поочередно** (например, перед удалением логировать их):

js

КопироватьРедактировать

`const fs = require('fs'); const path = require('path');  function deleteFolderRecursive(folderPath) {   if (fs.existsSync(folderPath)) {     fs.readdirSync(folderPath).forEach(file => {       const curPath = path.join(folderPath, file);       if (fs.lstatSync(curPath).isDirectory()) {         deleteFolderRecursive(curPath); // Удаляем подпапки рекурсивно       } else {         fs.unlinkSync(curPath); // Удаляем файлы       }     });     fs.rmdirSync(folderPath); // Удаляем пустую папку   } }  // Использование: deleteFolderRecursive('./myFolder'); console.log('Папка удалена');`

---

## 📝 Итог:

|Метод|Поддержка непустых папок|Асинхронный|Использовать в 2024?|
|---|---|---|---|
|`fs.rmdir()`|❌ Нет|✅ Да|❌ Устарел|
|`fs.rmdirSync()`|❌ Нет|❌ Нет|❌ Устарел|
|`fs.rm()`|✅ Да|✅ Да|✅ Рекомендуется|
|`fs.rmSync()`|✅ Да|❌ Нет|✅ Рекомендуется|
|Рекурсивный метод|✅ Да|❌ Нет|⏳ Если нужен контроль|

🔥 **Рекомендуемый способ:** `fs.rm('./folder', { recursive: true })` 🚀