---
title: Как получить информацию о процессорах системы в Node.js
draft: false
tags:
  - "#NodeJS"
  - "#os"
  - "#процессоры"
  - "#система"
  - "#cpus"
  - "#производительность"
info:
  - "[Документация Node.js по модулю os](https://nodejs.org/api/os.html)"
  - "[Документация по os.cpus()](https://nodejs.org/api/os.html#os_os_cpus)"
  - "[Документация по os.availableParallelism()](https://nodejs.org/api/os.html#os_os_availableparallelism)"
---

Для получения информации о процессорах используется модуль **`os`**.

### **1. Получение информации о каждом ядре процессора**

Метод **`os.cpus()`** возвращает массив объектов, содержащих информацию о каждом ядре.

#### **Пример: получение информации о процессорах**

```javascript
const os = require("os")

const cpus = os.cpus()
console.log("Количество ядер:", cpus.length)
console.log("Информация о первом ядре:", cpus[0])
```

📌 **Вывод:**

```json
{
  "model": "Intel(R) Core(TM) i7-9700 CPU @ 3.00GHz",
  "speed": 3000, // Скорость в MHz
  "times": {
    "user": 25235455, // Время в user mode
    "nice": 0, // Время с низким приоритетом (обычно 0)
    "sys": 14567890, // Время в system mode
    "idle": 102345678, // Время простоя
    "irq": 12345 // Время обработки прерываний
  }
}
```

- **`model`** – модель процессора.
- **`speed`** – тактовая частота в **MHz**.
- **`times`** – затраченное время в **миллисекундах** в разных режимах.

### **2. Количество логических ядер процессора**

```javascript
console.log("Количество логических ядер:", os.cpus().length)
```

### **3. Количество доступных потоков (виртуальных процессоров)**

Некоторые ядра поддерживают **гиперпоточность (Hyper-Threading)**, поэтому **количество логических ядер** может быть больше, чем физических.

#### **Пример: получение количества потоков**

```javascript
const { availableParallelism } = require("os")

console.log("Доступные потоки (виртуальные CPU):", availableParallelism())
```

📌 **Для современных систем `availableParallelism()` предпочтительнее, чем `os.cpus().length`, так как учитывает изоляцию ядер в контейнерах.**

### **Вывод**

✅ **`os.cpus()`** – подробная информация о каждом ядре.  
✅ **`os.cpus().length`** – общее количество логических ядер.  
✅ **`os.availableParallelism()`** – количество потоков, доступных для работы.

---

[[002 Node.js|Назад]]
