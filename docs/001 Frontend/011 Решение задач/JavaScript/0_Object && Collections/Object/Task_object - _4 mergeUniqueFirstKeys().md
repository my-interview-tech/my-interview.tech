---
title: mergeUniqueFirstKeys()
draft: false
tags:
  - "#JavaScript"
  - "#taskJS"
  - "#object"
---

```js
/**
_Функция объединяет объекты, сохраняя только первые уникальные ключи. 
Значения из последующих объектов игнорируются, если ключ уже существует. 
Аналог { ...obj1, ...obj2 }, 
но с обратным приоритетом (не перезаписывает существующие ключи).
*/

const objects = [
    { foo: 5, bar: 6 },
    { foo: 13, baz: -1 }
];

function mergeUniqueFirstKeys() {
	// Ваш код здесь
}

console.log(mergeUniqueFirstKeys(...objects)) ..
```

___

[[011 Решение задач JS, TS и React|Назад]]