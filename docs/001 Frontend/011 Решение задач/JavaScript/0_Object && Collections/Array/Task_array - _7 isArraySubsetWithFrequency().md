---
title: isArraySubsetWithFrequency()
draft: false
tags:
  - "#JavaScript"
  - "#taskJS"
  - "#array"
---
```JS
/*
Напишите функцию, которая проверяет, является ли второй массив подмножеством первого. То есть есть ли элементы второго массива в первом.
*/

function isArraySubsetWithFrequency(source, subset) {
	// Ваш код здесь
}

console.log(arraySubset([2, 1, 3], [1, 2, 3])) // -> true
console.log(arraySubset([2, 1, 1, 3], [1, 2, 3])) // -> true
console.log(arraySubset([1, 1, 1, 3], [1, 3, 3])) // -> false
console.log(arraySubset([1, 2], [1, 2, 3])) // -> false
```

___

[[011 Решение задач JS, TS и React|Назад]]